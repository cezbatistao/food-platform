# Food Platform Docker Compose

## docker-compose

### Start
```
docker-compose up -d --build
```

#### Access application
```
minikube ip
```
Open [`http://localhost:8080/`](http://localhost:8080/) to use application

### Stop application
```
docker-compose stop
```
