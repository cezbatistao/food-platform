spring:
  profiles:
    active: local

---

spring:
  config:
    activate:
      on-profile:
        - local
  cloud:
    kubernetes:
      enabled: false
      secrets:
        enabled: false
      config:
        enabled: false
      discovery:
        enabled: false
