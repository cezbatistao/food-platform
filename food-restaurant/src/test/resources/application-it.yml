spring:
  datasource:
    url: ${spring.datasource.restaurant.url.container}
    username: ${spring.datasource.restaurant.username.container}
    password: ${spring.datasource.restaurant.password.container}
